import React from 'react';
import { PlaceholderScreen } from '@/src/components/PlaceholderScreen';

export default function AppearanceScreen() {
  return (
    <PlaceholderScreen
      title="Appearance & theme"
      icon="palette"
      phase="Phase 1 · Theme system"
      description="Choose between light and dark mode. Bond follows your system preference by default."
    />
  );
}
