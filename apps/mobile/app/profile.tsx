import React from 'react';
import { PlaceholderScreen } from '@/src/components/PlaceholderScreen';

export default function ProfileScreen() {
  return (
    <PlaceholderScreen
      title="Profile"
      icon="person-outline"
      phase="Phase 2 · Onboarding & avatar setup"
      description="Edit your display name, Bond ID, avatar, bio, and privacy settings here once onboarding is complete."
    />
  );
}
