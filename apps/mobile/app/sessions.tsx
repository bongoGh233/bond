import React from 'react';
import { PlaceholderScreen } from '@/src/components/PlaceholderScreen';

export default function SessionsScreen() {
  return (
    <PlaceholderScreen
      title="Sessions & devices"
      icon="devices"
      phase="Phase 2 · Secure session management"
      description="Review and revoke active sessions and connected devices that can access your Bond account."
    />
  );
}
